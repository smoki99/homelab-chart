# Test values for infrastructure-base chart
sealedSecrets:
  enabled: true

postfix:
  enabled: true
  image: "boky/postfix"
  imageTag: "latest"
  service:
    type: ClusterIP
    port: 25
  config:
    domain: "example.com"
    hostname: "mail.example.com"
