# This file is now a library template that only manages dependencies
# Actual deployments are in their own charts (sealed-secrets and postfix)
# This placeholder ensures compatibility with existing references but doesn't deploy anything

# No actual service content needed for infrastructure-base as a library chart
