apiVersion: v1
kind: Service
metadata:
  name: {{ include "infrastructure-base.fullname" . }}
  labels:
    {{- include "infrastructure-base.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infrastructure-base.selectorLabels" . | nindent 4 }}
