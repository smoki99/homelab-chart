# This template file is intentionally left empty to maintain chart structure
# The actual deployments are managed by separate ArgoCD applications:
# - sealed-secrets application 
# - postfix application

# Infrastructure-base now acts purely as an orchestrator/dependency manager
